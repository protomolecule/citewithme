<%= form_for(@cite) do |f| %>
  <% if @cite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cite.errors.count, "error") %> prohibited this cite from being saved:</h2>

      <ul>
      <% @cite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :sauce %><br />
    <%= f.text_field :sauce %>
  </div>

  <div class="field">
    <%= f.label :page %><br />
    <%= f.text_field :page %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
 -------------------------

<%= form_for(@cite, :remote => true) do |g| %>
  <div class="field">
    <%= g.text_area :content %>
  </div>
  <div class="actions">
    <%= g.submit %>
  </div>
<% end %>
